apiVersion: v1
kind: Pod
metadata:
  name: resourcespod
spec:
  containers:
    - name: resourcepod
      image: nginx
      resources:
        requests:
          memory: "100Mi"
          cpu: "100m"
        limits:
          memory: "100Mi"
          cpu: "100m"